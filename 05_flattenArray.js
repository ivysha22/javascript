//write a function flattenArray that takes a nested array arr and returns a flattenArray containing all the elements in a nested arrays

//nested array--is an array that has  arrays inside itself array

const myArray=[1,2,3[32,43,34],90,[323,[43,34],3,2],[32,23]]//input

const expectedOutcome=[1,2,3,32,43,34,90,323,43,34,3,2,32,23]//expected output


//loop through the array in order to acces each element
//create anew empty array that will store our flattened array
//if an element is not an array add it to the flattened/result/final array 
//else if it is an array loop through it adding the resulting array
//when done return final array

function flattenArray(){
    let finalArray=[]
    for(let index=0;index<.length; index++){
        //check if the element at notflattenedInput is an array
        console.log()
    }
}


